---
layout: base.njk
title: Portfolio — Plume de l’Ombre
---

<section id=Textesenvers">
  <h1>Textes en vers</h1>
  <p class="muted">Sélection de travaux.</p>

  <div class="grid cols-3" role="list">
  {% for item in collections.redactions | reverse %}
    <article class="card" role="listitem">
      <h3>{{ item.data.title }}</h3>
      <p class="muted">Rédaction</p>
      <p>{{ item.templateContent | excerpt }}</p>
      <a href="{{ item.url }}" aria-label="Lire la rédaction complète">Lire le texte en ver complet.</a>
    </article>
  {% endfor %}
  </div>
</section>
