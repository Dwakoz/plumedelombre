<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Plume de l’ombre</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Plume de l’ombre — Atelier d’écriture poétique et moderne. Rédaction, correction, traduction, fiction.">
  <link rel="stylesheet" href="/styles.css">
</head>
<body>

<header>
  <div class="wrap">
    <nav>
      <div class="brand">Plume de l’ombre</div>
      <div class="menu">
        <a href="/">Accueil</a>
        <a href="#services">Services</a>

        <!-- Dropdown Portfolio -->
        <div class="dropdown" id="dd-portfolio">
          <button class="dropdown-toggle" aria-haspopup="true" aria-expanded="false" aria-controls="menu-portfolio" type="button">
            Portfolio <span class="arrow" aria-hidden="true"></span>
          </button>
          <div class="dropdown-menu" id="menu-portfolio" role="menu">
            <a href="#redaction" role="menuitem">Rédaction</a>
            <a href="#articles" role="menuitem">Articles</a>
            <a href="#fiction" role="menuitem">Fiction</a>
          </div>
        </div>

        <a href="#articles">Articles</a>
        <a href="#temoignages">Témoignages</a>
        <a href="#apropos">À propos</a>
        <a href="/contact" class="btn">Contactez-moi</a>
      </div>
    </nav>
  </div>
</header>

<main class="wrap">

  <!-- Hero -->
  <section class="hero">
    <h1>Bienvenue sur Plume de l’ombre</h1>
    <p class="muted">Atelier d’écriture. Rédaction, correction, traduction et fictions originales.</p>
    <a href="/contact" class="btn">Me contacter</a>
  </section>

  <!-- Services -->
  <section id="services">
    <h2>Services</h2>
    <div class="grid cols-3">
      <div class="card">
        <h3>Rédaction</h3>
        <p class="muted">Articles, récits, mémoires, scripts — clairs et impactants.</p>
      </div>
      <div class="card">
        <h3>Correction</h3>
        <p class="muted">Relecture, cohérence, style, orthotypographie.</p>
      </div>
      <div class="card">
        <h3>Traduction</h3>
        <p class="muted">Anglais ↔ Français, ton respecté.</p>
      </div>
    </div>
  </section>

  <!-- Portfolio / Rédaction -->
  <section id="redaction">
    <h2>Portfolio — Rédaction</h2>
    <div class="grid cols-2">
      <article class="card">
        <h3>Récit client A</h3>
        <p class="muted">Extrait court présentant le style narratif et la structure.</p>
        <span class="badge">récit</span>
      </article>
      <article class="card">
        <h3>White paper B</h3>
        <p class="muted">Document informatif synthétique pour décision rapide.</p>
        <span class="badge">pro</span>
      </article>
    </div>
  </section>

  <!-- Portfolio / Articles -->
  <section id="articles">
    <h2>Portfolio — Articles</h2>
    <ul class="list">
      {% for post in collections.post | reverse | slice(0,5) %}
        <li class="card">
          <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
          {% if post.date %}
            <p class="muted">
              <time datetime="{{ post.date | htmlDate }}">{{ post.date | readableDate }}</time>
            </p>
          {% endif %}
          {% if post.data.description %}<p>{{ post.data.description }}</p>{% endif %}
        </li>
      {% endfor %}
    </ul>
  </section>

  <!-- Portfolio / Fiction -->
  <section id="fiction">
    <h2>Portfolio — Fiction</h2>
    <div class="grid cols-2">
      <article class="card">
        <h3>Nouvelles courtes</h3>
        <p class="muted">Sélection de micro-fictions et concepts d’univers.</p>
        <span class="badge">fiction</span>
      </article>
      <article class="card">
        <h3>Extrait de roman</h3>
        <p class="muted">Chapitre d’ouverture dans un monde où l’on se souvient des vies passées.</p>
        <span class="badge">roman</span>
      </article>
    </div>
  </section>

  <!-- Témoignages -->
  <section id="temoignages">
    <h2>Témoignages</h2>
    <div class="grid cols-2">
      <div class="card">
        <p>“Texte livré propre et à l’heure. Bonne compréhension du brief.”</p>
        <p class="muted">— Client X</p>
      </div>
      <div class="card">
        <p>“Adaptabilité et style. Je recommande.”</p>
        <p class="muted">— Cliente Y</p>
      </div>
    </div>
  </section>

  <!-- À propos -->
  <section id="apropos">
    <h2>À propos</h2>
    <p class="muted">Profil, méthode de travail et valeurs. Contact possible par le formulaire.</p>
  </section>

</main>

<footer>
  <div class="wrap">
    <p>© Plume de l’ombre — Tous droits réservés.</p>
  </div>
</footer>

<script>
  // Dropdown accessible: clic pour ouvrir/fermer, fermeture au clic extérieur et via Échap
  (function(){
    const dd = document.getElementById('dd-portfolio');
    if(!dd) return;
    const btn = dd.querySelector('.dropdown-toggle');
    const menu = dd.querySelector('.dropdown-menu');

    function open(){ dd.classList.add('open'); btn.setAttribute('aria-expanded','true'); }
    function close(){ dd.classList.remove('open'); btn.setAttribute('aria-expanded','false'); }

    btn.addEventListener('click', (e)=> {
      e.preventDefault();
      dd.classList.toggle('open');
      btn.setAttribute('aria-expanded', dd.classList.contains('open') ? 'true' : 'false');
    });

    document.addEventListener('click', (e)=>{
      if(!dd.contains(e.target)) close();
    });

    document.addEventListener('keydown',(e)=>{
      if(e.key === 'Escape') close();
    });

    // Ferme après clic sur un item
    menu.addEventListener('click', (e)=>{
      if(e.target.tagName === 'A') close();
    });
  })();
</script>

</body>
</html>
