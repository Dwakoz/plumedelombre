---
layout: base.njk
title: Fiction — Plume de l’Ombre
permalink: /fiction.html
---

<section id="fiction">
  <h1>Fiction</h1>
  <div class="grid cols-3" role="list">
  {% for item in collections.fiction | reverse %}
    <article class="card" role="listitem">
      <h3 class="title-blue">{{ item.data.title | safe }}</h3>
      <p>
        {% if item.data.excerpt %}
          {{ item.data.excerpt | striptags | truncate(150, true, '…') }}
        {% else %}
          {{ item.templateContent | striptags | truncate(150, true, '…') }}
        {% endif %}
      </p>
      <p><a class="btn" href="{{ item.url }}" aria-label="Lire : {{ item.data.title | striptags }}">Lire</a></p>
    </article>
  {% endfor %}
  </div>
</section>

<style>
  .card h3.title-blue { color: var(--link, var(--accent, #3ea8ff)); }
</style>
