---
layout: base.njk
title: Fiction — Plume de l’Ombre
---

<section class="page-head">
  <h1>Fiction</h1>
</section>

<section class="listing" role="list">
  <div class="grid cols-3">
  {% for item in collections.fiction | reverse %}
    <article class="card" role="listitem">
      <h2><a href="{{ item.url }}">{{ item.data.title }}</a></h2>
      <p class="muted">
        {% if item.data.tags %}{{ item.data.tags | join(", ") }}{% endif %}
      </p>
      <p>
        {% if item.data.excerpt %}
          {{ item.data.excerpt | striptags | truncate(150, true, '…') }}
        {% else %}
          {{ item.templateContent | striptags | truncate(150, true, '…') }}
        {% endif %}
      </p>
      <a class="btn ghost" href="{{ item.url }}" aria-label="Lire : {{ item.data.title }}">Lire</a>
    </article>
  {% endfor %}
  {% if (collections.fiction | length) == 0 %}
    <p class="muted">Aucun texte de fiction pour le moment.</p>
  {% endif %}
  </div>
</section>
