<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Plume de l’ombre</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Plume de l’ombre — Atelier d’écriture. Rédaction, correction, traduction.">
  <link rel="stylesheet" href="/styles.css">
</head>
<body>

<header>
  <div class="wrap">
    <nav>
      <div class="brand">Plume de l’ombre</div>
      <div class="menu">
        <a href="/">Accueil</a>
        <a href="#services">Services</a>

        <!-- Portfolio devient un menu déroulant -->
        <div class="dropdown" id="dd-portfolio">
          <button class="dropdown-toggle" aria-haspopup="true" aria-expanded="false" aria-controls="menu-portfolio" type="button">
            Portfolio <span class="arrow" aria-hidden="true"></span>
          </button>
          <div class="dropdown-menu" id="menu-portfolio" role="menu">
            <a href="/portfolio" role="menuitem">Rédaction</a>
            <a href="/articles" role="menuitem">Articles</a>
          </div>
        </div>

        <!-- Lien Articles supprimé du niveau principal -->
        <!-- <a href="/articles">Articles</a> -->

        <a href="#temoignages">Témoignages</a>
        <a href="#apropos">À propos</a>
        <a href="/contact" class="btn">Contactez-moi</a>
      </div>
    </nav>
  </div>
</header>

<main class="wrap">

  <section class="hero">
    <h1>Bienvenue sur Plume de l’ombre</h1>
    <p class="muted">Atelier d’écriture. Rédaction, correction, traduction.</p>
    <a href="/contact" class="btn">Me contacter</a>
  </section>

  <section id="services">
    <h2>Services</h2>
    <div class="grid cols-3">
      <div class="card">
        <h3>Rédaction</h3>
        <p class="muted">Articles, récits, mémoires, scripts.</p>
      </div>
      <div class="card">
        <h3>Correction</h3>
        <p class="muted">Relecture, cohérence, style.</p>
      </div>
      <div class="card">
        <h3>Traduction</h3>
        <p class="muted">Anglais ↔ Français.</p>
      </div>
    </div>
  </section>

  <section id="articles">
    <h2>Articles récents</h2>
    <ul class="list">
      {% for post in collections.post | reverse | slice(0,5) %}
        <li class="card">
          <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
          {% if post.date %}
            <p class="muted">
              <time datetime="{{ post.date | htmlDate }}">{{ post.date | readableDate }}</time>
            </p>
          {% endif %}
          {% if post.data.description %}<p>{{ post.data.description }}</p>{% endif %}
        </li>
      {% endfor %}
    </ul>
  </section>

  <section id="temoignages">
    <h2>Témoignages</h2>
    <div class="grid cols-2">
      <div class="card"><p>“Texte livré propre et à l’heure.”</p><p class="muted">— Client X</p></div>
      <div class="card"><p>“Adaptabilité et style.”</p><p class="muted">— Cliente Y</p></div>
    </div>
  </section>

  <section id="apropos">
    <h2>À propos</h2>
    <p class="muted">Méthode, valeurs et contact.</p>
  </section>

</main>

<footer>
  <div class="wrap">
    <p>© Plume de l’ombre — Tous droits réservés.</p>
  </div>
</footer>

<script>
(function(){
  const dd=document.getElementById('dd-portfolio'); if(!dd) return;
  const btn=dd.querySelector('.dropdown-toggle'), menu=dd.querySelector('.dropdown-menu');
  const close=()=>{dd.classList.remove('open');btn.setAttribute('aria-expanded','false');};
  btn.addEventListener('click',e=>{e.preventDefault();dd.classList.toggle('open');btn.setAttribute('aria-expanded',dd.classList.contains('open')?'true':'false');});
  document.addEventListener('click',e=>{if(!dd.contains(e.target)) close();});
  document.addEventListener('keydown',e=>{if(e.key==='Escape') close();});
  menu.addEventListener('click',e=>{if(e.target.tagName==='A') close();});
})();
</script>

</body>
</html>
