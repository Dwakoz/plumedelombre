---
layout: base.njk
title: Accueil — Plume de l’Ombre
---

<section class="hero" aria-labelledby="hero-title">
  <p class="badge">FR / EN</p>
  <h1 id="hero-title">Vos idées, mes mots.</h1>
  <p>Mes mots donnent vie à vos idées. // Ghostwriter polyvalente, je rédige pour vous tout type de textes, de la fiction aux chansons, en passant par les scripts vidéo et les articles scientifiques. Je peux également traduire des textes de l'anglais vers le français.
</p>
  <div style="display:flex;gap:12px;margin-top:10px">
    <a class="btn" href="/contact.html" aria-label="Contacter Plume de l’Ombre pour discuter d’un projet">Contactez-moi</a>
    <a class="btn ghost" href="/textes.html" aria-label="Voir des textes en vers">Voir des textes</a>
  </div>
</section>

<section id="textes" aria-labelledby="txt-title">
  <h2 id="txt-title">Textes en vers</h2>
  <div class="grid cols-3" role="list">
  {% for item in collections.textes | reverse %}
    {% if loop.index0 < 6 %}
      <article class="card" role="listitem">
        <h3><a href="{{ item.url }}">{{ item.data.title }}</a></h3>
        <p class="muted">Texte</p>
        <p>{{ item.templateContent | striptags | truncate(150, true, '…') }}</p>
        <a class="btn ghost" href="{{ item.url }}" aria-label="Lire : {{ item.data.title }}">Lire</a>
      </article>
    {% endif %}
  {% endfor %}
  {% if (collections.textes | length) == 0 %}
    <p class="muted">Aucun texte pour le moment.</p>
  {% endif %}
  </div>
</section>

<section id="fiction" aria-labelledby="fic-title">
  <h2 id="fic-title">Fiction</h2>
  <div class="grid cols-3" role="list">
  {% for item in collections.fiction | reverse %}
    {% if loop.index0 < 6 %}
      <article class="card" role="listitem">
        <h3><a href="{{ item.url }}">{{ item.data.title }}</a></h3>
        <p class="muted">
          {% if item.data.tags %}{{ item.data.tags | join(", ") }}{% else %}Nouvelle{% endif %}
        </p>
        <p>
          {% if item.data.excerpt %}
            {{ item.data.excerpt | striptags | truncate(150, true, '…') }}
          {% else %}
            {{ item.templateContent | striptags | truncate(150, true, '…') }}
          {% endif %}
        </p>
        <a class="btn ghost" href="{{ item.url }}" aria-label="Lire : {{ item.data.title }}">Lire</a>
      </article>
    {% endif %}
  {% endfor %}
  {% if (collections.fiction | length) == 0 %}
    <p class="muted">Aucun texte de fiction pour le moment.</p>
  {% endif %}
  </div>
</section>

<section id="articles" aria-labelledby="art-title">
  <h2 id="art-title">Articles publiés en ligne</h2>
  <div class="grid cols-3" role="list">
  {% for article in collections.articles | reverse %}
    {% if loop.index0 < 6 %}
      <article class="card" role="listitem">
        <h3>{{ article.data.title }}</h3>
        <p class="muted">{{ article.data.publisher or '' }}</p>
        <p>{{ (article.data.excerpt or '') | striptags | truncate(150, true, '…') }}</p>
        <p>
          <a class="btn ghost" href="{{ article.data.url }}" target="_blank" rel="noopener"
             aria-label="Lire l’article externe : {{ article.data.title }}">Lire</a>
        </p>
      </article>
    {% endif %}
  {% endfor %}
  {% if (collections.articles | length) == 0 %}
    <p class="muted">Aucun article référencé pour le moment.</p>
  {% endif %}
  </div>
</section>
