---
layout: base.njk
title: Accueil — Plume de l’Ombre
---

<section class="hero" aria-labelledby="hero-title">
  <p class="badge">FR / EN</p>
  <h1 id="hero-title">Vos idées, mes mots.</h1>
  <p>Ghostwriter bilingue FR/EN. J’écris à votre place, dans votre style.</p>
  <div style="display:flex;gap:12px;margin-top:10px">
    <a class="btn" href="/contact.html" aria-label="Contacter Plume de l’Ombre pour discuter d’un projet">Contactez-moi</a>
    <a class="btn ghost" href="/portfolio.html" aria-label="Voir toutes mes rédactions">Voir des rédactions</a>
  </div>
</section>

<section id="portfolio" aria-labelledby="redac-title">
  <h2 id="redac-title">Dernières rédactions</h2>
  <div class="grid cols-3" role="list" id="redactions-dyn">
  {% for item in collections.redactions | reverse %}
    {% if loop.index0 < 6 %}
      <article class="card" role="listitem">
        <h3>{{ item.data.title }}</h3>
        <p class="muted">Rédaction</p>
        <p>{{ item.templateContent | excerpt }}</p>
        <a href="{{ item.url }}" aria-label="Lire la rédaction complète : {{ item.data.title }}">Lire la rédaction complète</a>
      </article>
    {% endif %}
  {% endfor %}
  {% if (collections.redactions | length) == 0 %}
    <p class="muted">Aucune rédaction publiée pour le moment.</p>
  {% endif %}
  </div>
</section>

<section id="articles" aria-labelledby="art-title">
  <h2 id="art-title">Articles publiés en ligne</h2>
  <div class="grid cols-3" role="list">
  {% for article in collections.articles | reverse %}
    {% if loop.index0 < 6 %}
      <article class="card" role="listitem">
        <h3>{{ article.data.title }}</h3>
        <p class="muted">{{ article.data.publisher or '' }}</p>
        <p>{{ article.data.excerpt or '' }}</p>
        <p>
          <a class="btn ghost" href="{{ article.data.url }}" target="_blank" rel="noopener"
             aria-label="Lire l’article externe : {{ article.data.title }}">Lire</a>
        </p>
      </article>
    {% endif %}
  {% endfor %}
  {% if (collections.articles | length) == 0 %}
    <p class="muted">Aucun article référencé pour le moment.</p>
  {% endif %}
  </div>
</section>
